"""Concrete plugin implementations.

Each subpackage corresponds to a `shared/protocols/*` Protocol category.
Plugins register themselves with `shared.protocols.registry.registry` on
import so the config loader can find them by name.
"""
